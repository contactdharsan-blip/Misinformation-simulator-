base: world_baseline.yaml

sim:
  n_agents: 50000
  n_claims: 1
  n_steps: 365
  snapshot_interval: 30
  seed: 42
  device: auto
  deterministic: true
  seed_fraction: 0.001
  adoption_threshold: 0.8

world:
  moderation_strictness: 0.85
  platform_friction: 0.4
  debunk_intensity: 0.35
moderation:
  warning_effect: 0.5
  downrank_effect: 0.55
